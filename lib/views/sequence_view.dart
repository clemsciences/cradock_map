import "package:flutter/material.dart";

import "map_view.dart";

class SequenceView extends StatelessWidget {
  const SequenceView({super.key});

  @override
  Widget build(BuildContext context) {
    return MapView(
      title: "Cradock - séquence",
    );
  }
}
