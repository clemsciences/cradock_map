import "package:flutter/material.dart";

class SequenceItemView extends StatelessWidget {
  const SequenceItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
